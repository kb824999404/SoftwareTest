spring:
  resources:
      static-locations: classpath:static/,file:${spring.servlet.multipart.location}
  mvc:
      static-path-pattern: /**
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 500MB
  
server:
  port: 80
